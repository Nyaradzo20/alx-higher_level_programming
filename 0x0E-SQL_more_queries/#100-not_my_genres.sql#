-- script that uses the hbtn_0d_tvshows database to lists all genres not linked to Dexter

 SELECT tv_genres.name
       FROM tv_genres
            WHERE tv_genres.name NOT IN (
	        SELECT tv_genres.name
		      FROM tv_genres
		      INNER JOIN tv_show_genres
		              ON tv_genres.id = tv_show_genres.genr